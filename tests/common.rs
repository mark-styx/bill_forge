// Shared test utilities and fixtures
mod common {
    pub fn setup() -> () {
        // Setup code for all tests
    }

    pub fn teardown() -> () {
        // Teardown code for all tests
    }
}

pub use common::*;